export interface IGetPreApprovedSumecCommand {
  /**
   * CI of user to find in sumec api.
   *
   * @type {string}
   * @memberof IGetPreApprovedSumecCommand
   */
  ci: string;
  /**
   * Age of user to find in sumec api.
   *
   * @type {string}
   * @memberof IGetPreApprovedSumecCommand
   */
  age: string;
  /**
   * Salary of user to find in sumec api.
   *
   * @type {string}
   * @memberof IGetPreApprovedSumecCommand
   */
  salary: string;
}
